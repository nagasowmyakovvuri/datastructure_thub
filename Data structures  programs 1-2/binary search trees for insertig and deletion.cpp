#include<stdio.h>
int main()
{
	
}

/*create a new node with a value
temp=root
temp1=None
while temp
{
	if (val==temp->data)
	{
		print the value is already in the list
	}
	else if(val<temp=>data)if value is less than the root node means than we have to move to left
	{
		temp1=temp
		temp=temp->left
	}
	else-->if value is greater than the root node means than we have to move to right
	{
		temp1=temp
		temp=temp->right
	}
}
if temp1->data>val
{
	temp1->left=NN
}
else
{
	temp1->right=NN
}*/
