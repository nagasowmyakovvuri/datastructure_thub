#include <stdio.h>
#include<stdlib.h>
int top=-1;

int main()
{
	int *st,i,n,ch,val;
	scanf("%d",&n);
	st=(int*)calloc(n*sizeof(int));
	while(1)
	{
		printf("1.push 2.pop 3.display 4.exit");
		scanf("%d",&ch);
		if (ch==1)
		{
			scanf("%d")
		}
		else if(ch==2)
		{
			//pop
		}
		else if(ch==3)
		{
			//display
		}
		else
		{
			break;
		}
	}
	return 0;
}

